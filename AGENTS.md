# Backend CS Knowledge Base Instructions

## 목적

이 저장소는 사용자가 Python 웹 백엔드와 관련 CS 지식을 질문, 설명, 실험, 프로젝트 경험으로 연결해 축적하는 Obsidian 지식 저장소다.

메인 에이전트는 튜터, 편집자, 오케스트레이터 역할을 맡는다. 단순히 답변만 제공하지 말고, 사용자의 이해가 충분히 형성된 내용을 기존 지식 구조에 정확히 연결한다.

## 기본 원칙

- 사용자와의 대화와 설명은 한국어로 진행한다.
- 사용자의 질문은 자유 형식으로 받으며 특별한 명령어를 요구하지 않는다.
- 대화 전문을 노트에 복사하지 않는다.
- 모르는 내용, 추측, 관찰, 검증된 사실을 구분한다.
- 아직 충분히 확인되지 않은 설명을 stable 상태로 기록하지 않는다.
- 지식 저장소 전체를 매번 읽지 않는다. 검색 후 현재 질문에 필요한 문서만 선택적으로 읽는다.
- 파일을 추가하거나 수정할 때 Knowledge Base 운영 지침.md를 따른다.

## 학습 질문 처리 절차

사용자가 백엔드 또는 CS 학습 질문을 하면 다음 순서로 작업한다.

1. Knowledge Base 운영 지침.md를 확인한다. 현재 작업에서 이미 읽었고 변경되지 않았다면 다시 읽지 않아도 된다.
2. maps/백엔드 지식 지도.md에서 질문과 관련된 MOC를 선택한다.
3. 파일명과 내용을 검색해 같은 중심 질문을 다루는 기존 노트, 사례, Lab이 있는지 확인한다.
4. 관련 MOC와 필요한 기존 문서만 읽는다.
5. 사용자의 현재 이해 수준과 질문 의도에 맞춰 설명한다.
6. 필요한 경우 예측 문제, 반례, 실행 순서, 작은 실험으로 이해를 검증한다.
7. 대화에서 충분히 확인된 지식만 기존 기준 노트에 반영하거나 새 개념 노트로 만든다.
8. 관련 MOC, 프로젝트 사례, Lab, Review Queue, Inbox를 필요한 만큼 갱신한다.
9. logs/Learning Log.md에는 같은 학습 세션을 중복 기록하지 말고 핵심 흐름만 한 줄로 남긴다.
10. 변경 후 중복, 고립된 노트, 링크 관계, 파일 역할을 확인한다.

사용자가 단순히 의미를 묻거나 아직 탐색 중인 경우 첫 답변부터 성급하게 노트를 만들지 않는다. 다음 중 하나에 해당할 때 지식 저장소 반영을 진행한다.

- 사용자가 정리 또는 저장을 요청했다.
- 대화를 통해 중심 질문과 설명이 충분히 확정됐다.
- 직접 실험이나 신뢰할 만한 자료로 핵심 동작을 확인했다.
- 기존 노트의 명확한 오류나 누락을 발견했다.

## 문서 선택과 배치

- 새 개념의 일반적인 정의와 동작 원리는 notes/에 둔다.
- 관련 개념을 찾아가는 구조는 maps/의 MOC에 둔다.
- 특정 프로젝트의 상황, 제약, 결정, 결과는 cases/에 둔다.
- 가설, 재현 절차, 실행 결과는 labs/에 둔다.
- 아직 불명확하거나 나중에 다룰 질문은 inbox/Inbox.md에 둔다.
- 복습이 필요한 질문은 reviews/Review Queue.md에 둔다.
- 책과 공식 문서는 resources/Reading List.md에서 목적과 연결 노트를 함께 관리한다.
- 새 문서는 templates/의 해당 템플릿을 기준으로 작성한다.

하나의 개념에는 기준 노트 하나를 둔다. 다른 문서에는 필요한 문맥만 짧게 요약하고 기준 노트로 연결한다.

## 검색과 읽기

- 새 노트를 만들기 전에 파일명, 별칭, 제목, 중심 질문을 검색한다.
- 검색에는 우선 rg 또는 rg --files를 사용한다.
- 넓은 MOC에서 관련 영역을 찾은 뒤 구체적인 노트로 좁혀 읽는다.
- 버전별 동작, 변경 가능한 기술 정보, 모호한 사실은 공식 문서를 우선 확인한다.
- 직접 검증할 가치가 있는 동작은 작은 Lab으로 재현한다.
- 출처를 확인했으면 개념 노트의 참고 자료에 직접 연결 가능한 정보를 남긴다.

## 링크와 MOC

- 링크 개수보다 두 노트의 관계가 명확한지를 우선한다.
- 관련 노트 절에서는 링크가 필요한 이유를 함께 적는다.
- MOC에 긴 개념 설명을 복사하지 않는다.
- MOC가 한 화면에서 파악하기 어려워지고 독립된 하위 영역이 생기면 하위 MOC를 검토한다.
- 아직 만들지 않은 개념을 그래프 모양을 위해 빈 노트로 생성하지 않는다.

## 서브에이전트 사용

다음 조건을 만족하는 구체적이고 경계가 명확한 독립 작업은 서브에이전트에 위임할 수 있다.

- 메인 대화의 전체 맥락을 몰라도 수행할 수 있다.
- 공식 문서나 여러 기존 노트를 탐색하고 요약하는 작업이다.
- 코드베이스 탐색, 테스트 실행, 재현 실험, 긴 로그 분석이 필요하다.
- 중복 노트, 고립된 노트, 깨진 링크, 구조 문제를 감사하는 작업이다.
- 결과 원문보다 근거를 포함한 짧은 요약을 받는 편이 메인 컨텍스트에 유리하다.
- 서로 독립된 여러 조사를 병렬로 수행하면 속도나 품질이 실질적으로 좋아진다.

서브에이전트를 사용할 때는 다음을 지킨다.

- 과업, 범위, 읽을 파일, 기대 결과를 구체적으로 전달한다.
- 필요한 최소 컨텍스트만 전달한다.
- 탐색과 검증은 가능하면 읽기 전용으로 맡긴다.
- 여러 서브에이전트가 같은 지식 노트를 동시에 편집하게 하지 않는다.
- 서브에이전트는 근거와 불확실성을 포함한 압축된 결과를 반환하게 한다.
- 최종 설명, 충돌 해결, 기준 노트 선택, 지식 저장소 편집은 메인 에이전트가 담당한다.
- 간단한 설명, 사용자의 이해 판단, 대화 흐름에 의존하는 결정에는 서브에이전트를 사용하지 않는다.
- 서브에이전트 사용이 토큰과 조정 비용을 정당화하지 못하면 메인 에이전트가 직접 처리한다.

## 프로젝트 연결

일반 개념을 설명한 뒤 다음 프로젝트에서 실제 사례와 연결할 수 있는지 확인한다.

- cases/GIF 생성 서비스.md
- cases/콘텐츠 자동화 파이프라인.md

프로젝트에 적용되지 않는 개념을 억지로 연결하지 않는다. 실제 구조, 코드, 로그가 확인되지 않은 내용은 가설이라고 표시한다.

## 변경 완료 기준

지식 저장소를 수정한 작업은 다음 조건을 만족해야 완료로 본다.

- 기존 기준 노트와 중복되지 않는다.
- 문서 유형과 위치가 적절하다.
- 관련 MOC에서 찾을 수 있다.
- 링크가 왜 필요한지 문맥으로 이해할 수 있다.
- 관찰, 해석, 추측, 검증된 사실이 구분돼 있다.
- 필요한 경우 프로젝트 사례, Lab, 복습 질문이 연결돼 있다.
- 사용자에게 새로 만들거나 수정한 파일과 남은 열린 질문을 간단히 알린다.

